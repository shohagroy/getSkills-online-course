import React from 'react';
import Navigation from './Navigation'
import Hero from './Hero'

const Header = () => {
    return (
        <div>
            <Navigation/>
            <Hero/>
        </div>
    );
};

export default Header;